<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.tidus.ristrat">

    <!-- 允许应用程序改变网络状态 -->

    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <!-- 允许应用程序改变WIFI连接状态 -->

    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!-- 允许应用程序访问有关的网络信息 -->

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- 允许应用程序访问WIFI网卡的网络信息 -->

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- 允许应用程序完全使用网络 -->

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".application.App"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        tools:replace="android:icon">
        <activity
            android:name=".mvp.view.LogActivity"
            android:theme="@style/TransparentTheme"></activity>
        <activity android:name=".mvp.view.HistoryAssessActivity"></activity>
        <activity
            android:name=".mvp.view.CancelQuestionActivity"
            android:theme="@style/TransparentTheme"></activity>
        <activity android:name=".mvp.view.RiskAssessmentActivity"></activity>
        <activity
            android:name=".mvp.view.SelectQuestionActivity"
            android:theme="@style/TransparentTheme"></activity>
        <activity android:name=".mvp.view.MessageActivity"></activity>
        <activity android:name=".mvp.view.CaseControlActivity"></activity>
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden"
            android:windowSoftInputMode="adjustUnspecified|stateHidden" />
        <activity
            android:name=".mvp.view.UserLoginActivity"
            android:configChanges="orientation|keyboardHidden"
            android:windowSoftInputMode="adjustResize|adjustUnspecified|stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".mvp.view.RuestionnaireActivity"
            android:configChanges="orientation|keyboardHidden"
            android:windowSoftInputMode="adjustUnspecified|stateHidden" />
        <activity android:name=".mvp.view.RiskActivity" />
        <activity android:name=".mvp.view.ReportActivity" />
        <activity android:name=".mvp.view.InfromActivity" />
        <activity
            android:name=".mvp.view.ForgetPwdActivity"
            android:configChanges="orientation|keyboardHidden"
            android:windowSoftInputMode="adjustUnspecified|stateHidden">>
        </activity>


    </application>


</manifest>